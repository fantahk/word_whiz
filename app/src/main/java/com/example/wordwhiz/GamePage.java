package com.example.wordwhiz;

import android.widget.Button;
import java.util.Random;

public class GamePage {
    /*
    public void getRandomAnswer() {
        Random random = new Random();
        Button[] answers = new Button[4];
        int randomInt = random.nextInt(4);
        answers[0] = (Button) findViewById(R.id.answer0);
        answers[1] = (Button) findViewById(R.id.answer1);
        answers[2] = (Button) findViewById(R.id.answer2);
        answers[3] = (Button) findViewById(R.id.answer3);
        Button correctAnswer = answers[randomInt];
        correctAnswer.setText("Correct Word"); // Will turn into API Request for Random Word that connects to definition.

    }

 */
}
